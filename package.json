{
    "name": "delete-test-01",
    "description": "delete-test-01-description",
    "version": "0.0.1",
    "private": true,
    "dependencies": {
        "aws-sdk": "^2.814.0"
    },
    "devDependencies": {
        "jest": "^24.7.1"
    },
    "scripts": {
        "test": "jest",
        "params": "python ./utilities/build_params.py temple > env.json",
        "deploy": "sam build && sam deploy && rm -rf .sam-aws"
    }
}
